<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blogbasbas.harike4">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ListWisataActivity"
            android:label="List Wisata "
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".activity.SpinnerActivity"
            android:label="Spinner Activity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".activity.DetailActivity"
            android:label="Detail Activity "
            android:parentActivityName=".activity.ListWisataActivity" />
        <activity
            android:name=".activity.ViewPagerActivity"
            android:label="ViewPager Activity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".activity.VideoActivity" />
        <activity android:name=".activity.RadioActivity"></activity>
    </application>

</manifest>